blockを実装している時のまとめ

Node* node {
    ...
    Node **blocks;
    ...
};

この構造体のblocksメンバの扱い、
- どういう構造体なのか
- node->のようにアクセスするときはどうやって書くものなのか。

がいまいちピンと来てなかったため調査

Node *blocks[];

って書き方は、”フレキシブル配列メンバ”って呼ぶらしい、これは構造体の一番したの要素にのみ使うことができて、定義するときに、配列の要素数を変更して割り当てることができる。

